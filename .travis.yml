language: node_js
node_js:
  - "12"
dist: trusty
sudo: required
branches:
  only:
    - master

jobs:
  include:
    - stage: lint
      script: npm run lint
    - stage: test
      script: npm run test
    - stage: build
      script: npm run build
